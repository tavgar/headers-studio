{
  "name": "headers-studio",
  "version": "1.0.0",
  "private": true,
  "description": "A full-page Chrome extension for inspecting, copying, and overriding HTTP headers.",
  "scripts": {
    "check": "node --check extension/background.js && node --check extension/panel.js && node --check extension/lib/headers.js && node --check extension/lib/rules.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
